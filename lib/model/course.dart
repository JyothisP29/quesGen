import 'dart:core';

import 'dart:ui';

class Course {
  int id;
  String? name;
  String? university;
  Color color;
  Color borderColor;

  Course(this.id, this.name, this.university, this.color, this.borderColor);
}
