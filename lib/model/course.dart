import 'dart:core';

import 'dart:ui';

class Course {
  int id;
  String? type;

  String? university;
  String? name;
  Color color;
  Color borderColor;

  Course(this.id, this.type,this.university,this.name,  this.color, this.borderColor);
}
